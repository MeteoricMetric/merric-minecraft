# Overworld render. Cosmic theme — dark sky.
world: "world"
dimension: "minecraft:overworld"
name: "MeteoricCraft Overworld"

sky-color: "#0a0a2e"
void-color: "#000010"
ambient-light: 0.1

# No marker sets — per docs/CHILD-SAFETY-PRIVACY.md §2.2 (no live player
# markers, no claim outlines until explicitly enabled).
marker-sets: {}

render-edges: true
save-hires-layer: true
