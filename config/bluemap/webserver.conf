# BlueMap webserver — bind 0.0.0.0:8100 inside container; compose forwards
# 127.0.0.1:8100 on host. Public URL eventually map.merricstrough.com via DNS.

enabled: true
webroot: "web"
ip: "0.0.0.0"
port: 8100
max-connection-count: 100
